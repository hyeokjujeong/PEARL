# Modernized dependency set for PEARL (2026).
#
# Replaces the bit-rotted docker/environment.yml (python 3.5 / torch 1.0.1 /
# mujoco-py 1.50). Tested on Ubuntu, conda env `pearl` with python 3.11 and an
# RTX 5070 Ti (Blackwell). MuJoCo is now the DeepMind `mujoco` package, so no
# roboti.us license key is required.
#
# Setup:
#   conda create -n pearl python=3.11 -y
#   conda activate pearl
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements-modern.txt
#
# Note: torch/torchvision are installed separately (above) from the CUDA 12.8
# wheel index — Blackwell GPUs need CUDA >= 12.8 / torch >= 2.7.

gymnasium==1.3.0
mujoco==3.8.1
numpy==2.4.4
scipy==1.17.1
matplotlib==3.10.9
pandas==3.0.3
gtimer==1.0.0b5
click==8.3.3
python-dateutil==2.9.0.post0
joblib==1.5.3
cloudpickle==3.1.2
imageio==2.37.3
imageio-ffmpeg==0.6.0
moviepy==2.2.1
